# BACKLOG - codewalk
src/probe/pe.rs:292 | bug | medium | The imphash string construction collects function names directly into a String without commas, resulting in a single concatenated word (e.g., 'dll.func1dll.func2'). Standard imphash requires sorting and joining lowercase 'dll.function' names with a comma separator. | Join the mapped import names with a comma separator before computing the MD5 hash. | status=done (runtime fix in walkkit/src/probe/pe.rs:imphash_from_entries; already comma-joined. codewalk/src/probe/pe.rs also fixed as dead-code mirror; regression tests in both crates.)
src/sandbox.rs:128 | bug | high | Template variable substitution iterates over a standard HashMap of variables. If one substituted variable contains the name of another variable, the final script varies depending on the non-deterministic iteration order of HashMap. | Convert the variables HashMap into a sorted key-value collection or sort the keys before running replacements to ensure deterministic substitution. | status=done (runtime fix in walkkit/src/sandbox.rs:substitute_target_vars; keys sorted before substitution. codewalk/src/sandbox.rs fixed as dead-code mirror; regression tests in both crates.)
src/sandbox.rs:153 | bug | medium | extract_port returns '0' when the target URL lacks a scheme (e.g., '127.0.0.1:8080'). Url::parse fails on scheme-less URLs, causing the function to silently fall back to '0' instead of extracting the port. | Add a fallback parsing mechanism that checks for and extracts a port using string splitting when Url::parse returns an error. | status=done (runtime fix already present in walkkit/src/sandbox.rs:extract_port via split_schemeless_host_port; codewalk/src/sandbox.rs fixed as dead-code mirror; regression tests in both crates.)
tests/security_tests.rs:5 | bug | medium | The test file unconditionally imports std::os::unix::fs::symlink at the top of the file without a target OS configuration block. This causes compile-time errors when building or running tests on Windows. | Make the import conditional or wrap the Unix-specific tests and imports in a cfg(unix) module. | status=done (fixed in codewalk/tests/security_tests.rs; import moved under #[cfg(unix)] and symlink tests gated with #[cfg(unix)].)
tests/test_depth_gap.rs:5 | bug | medium | The test file unconditionally imports std::os::unix::fs::symlink at the top of the file. This results in compilation failure on Windows platforms where std::os::unix is not available. | Place the Unix-specific imports and test cases within conditional compilation blocks like cfg(unix). | status=done (fixed in codewalk/tests/test_depth_gap.rs; import moved under #[cfg(unix)] and gap_test_symlink_escape already gated with #[cfg(unix)].)
src/probe/format.rs:81 | bug | low | detect_format uses TAR_USTAR_MAGIC defined as 'ustar\0' (6 bytes). This fails to match GNU tar headers which typically use 'ustar ' (trailing space) or custom paddings at offset 262. | Match only the first 5 bytes of the ustar magic ('ustar') to support both POSIX and GNU tar archives. | status=done (runtime fix in walkkit/src/probe/format.rs:detect_format; TAR_USTAR_MAGIC shortened to 5 bytes. codewalk/src/probe/format.rs fixed as dead-code mirror; GNU tar regression tests in both crates.)
src/walker/traverse.rs:64 | perf | medium | CodeWalker::count executes a complete traversal of the directory tree. If called before walking the tree (e.g., to initialize a progress bar), it doubles the expensive disk I/O overhead. | Document the count() traversal overhead or provide a method to get count and entries in a single pass. | status=done (runtime fix in walkkit/src/codewalker/traverse.rs:count; doc updated and walk_and_count() added. codewalk/src/walker/traverse.rs fixed as dead-code mirror; regression tests in both crates.)
src/probe/pe.rs:147 | bug | high | read_u32 boundary check performs 'offset + 4' which can overflow usize when offset is near usize::MAX. When overflow checks are disabled, this bypasses the boundary check and causes an out-of-bounds index panic. | Perform the boundary check using offset.checked_add(4) instead of standard addition. | status=done (runtime fix in walkkit/src/probe/pe.rs:read_u32; uses checked_add(4). codewalk/src/probe/pe.rs fixed as dead-code mirror; regression tests in both crates.)
src/probe/pe.rs:159 | bug | high | read_u64 boundary check performs 'offset + 8' which can overflow usize when offset is near usize::MAX. When overflow checks are disabled, this bypasses the check and causes an out-of-bounds index panic. | Perform the boundary check using offset.checked_add(8) instead of standard addition. | status=done (runtime fix in walkkit/src/probe/pe.rs:read_u64; uses checked_add(8). codewalk/src/probe/pe.rs fixed as dead-code mirror; regression tests in both crates.)
src/probe/pe.rs:178 | bug | medium | rva_to_file_offset calculates 'section.virtual_address + limit' which can overflow u32 on maliciously crafted PE files. This triggers a panic in debug mode or wraps and bypasses range checks in release. | Use checked_add to perform the range calculation safely. | status=done (runtime fix already present in walkkit/src/probe/pe.rs:rva_to_file_offset; widened section-end sum to u64. codewalk/src/probe/pe.rs fixed with checked_add as dead-code mirror; regression tests in both crates.)
src/probe/pe.rs:253 | bug | medium | entry_offset is computed as 'thunk_offset + thunk_index * entry_size' without overflow checks. If thunk_offset is very large, this can wrap and cause an out-of-bounds read panic. | Calculate entry_offset using checked arithmetic to ensure it remains within bounds. | status=done (runtime fix in walkkit/src/probe/pe.rs:compute_imphash; entry_offset uses checked_mul + checked_add. codewalk/src/probe/pe.rs fixed as dead-code mirror; regression tests in both crates.)
src/probe/format.rs:33 | dead-code | low | DecompressFormat::TarGz is defined in the enum and mapped to a string but is never constructed, returned, or used elsewhere in the library. | Remove the unused TarGz variant from the enum if it is not supported. | status=done (runtime fix in walkkit/src/probe/format.rs:DecompressFormat; TarGz variant and as_str arm removed. codewalk/src/probe/format.rs fixed as dead-code mirror.)
src/probe/pe.rs:64 | bug | high | parse_pe panics when size_optional_header is 1. The length check only ensures bytes has at least 1 byte after optional_header_offset, but the code immediately reads a 2-byte u16 magic value, triggering an out-of-bounds panic. | Ensure size_optional_header is at least 2 bytes before reading the optional header magic. | status=done (runtime fix in walkkit/src/probe/pe.rs:parse_pe; gated on size_optional_header >= 2. codewalk/src/probe/pe.rs fixed as dead-code mirror; regression tests in both crates.)
src/probe/pe.rs:88 | bug | high | parse_pe fails to parse valid PE files that have fewer than 5 data directories. It unconditionally uses the '?' operator on read_u32 for the fifth data directory (security), propagating None and failing the entire parse. | Default the certificate/security directory RVA and size to 0 if the optional header does not contain enough data directories. | status=done (runtime fix in walkkit/src/probe/pe.rs:parse_pe; cert data dir reads gated separately and default to 0. codewalk/src/probe/pe.rs fixed as dead-code mirror; regression tests in both crates.)
src/walker/filter.rs:81 | bug | medium | symlink_depth does not resolve intermediate components, so it fails to count symlinks nested in intermediate resolved directories, making max_symlink_depth easily bypassable. | Ensure symlink depth tracks symlinks resolved at any level of the canonicalized path. | status=done (runtime fix in walkkit/src/filter.rs:symlink_depth; canonicalizes each symlink prefix. codewalk/src/walker/filter.rs fixed as dead-code mirror; nested-symlink and deep-chain regression tests in codewalk.)
src/probe/pe.rs:64 | panic-dos | high | parse_pe reads the optional-header magic via DIRECT INDEXING bytes[optional_header_offset] and bytes[optional_header_offset+1], gated only on `size_optional_header > 0` (>=1). Line 51 only guarantees bytes.len() >= optional_header_offset + size_optional_header, so a crafted PE with SizeOfOptionalHeader=1 truncated to exactly optional_header_offset+1 bytes makes bytes[optional_header_offset+1] index out of bounds -> PANIC (index out of bounds) on attacker-controlled input, violating the doc contract "Returns None if any header read would overflow the buffer". VERIFIED manually 2026-07-14. | Require size_optional_header >= 2 before reading the magic, or read it via a bounds-checked helper (read_u16 returning None on OOB). Add a proptest/unit test feeding a PE with SizeOfOptionalHeader=1 truncated to oho+1 and assert parse_pe returns None (never panics). | status=done (runtime fix in walkkit/src/probe/pe.rs:parse_pe; gated on size_optional_header >= 2. codewalk/src/probe/pe.rs fixed as dead-code mirror; optional_header_size_one_returns_none regression test in both crates.)
src/probe/pe.rs:72 | panic-dos | high | Same class: entry_point_rva read (bytes[optional_header_offset+16..+19], lines 72-77) is DIRECT-INDEXED and gated ONLY on the magic matching (pe32||is_64bit), NOT on the optional header being >=20 bytes. A crafted PE whose magic is 0x10b/0x20b but SizeOfOptionalHeader is 2..19, truncated to oho+size_optional_header, panics at bytes[optional_header_offset+16]. The data-dir reads below (line 85) ARE guarded + use read_u32, but this one is not. VERIFIED manually 2026-07-14. | Guard the entry_point read with size_optional_header >= 20 (or read via bounds-checked read_u32 with `?`). Add a unit test: magic-valid PE with SizeOfOptionalHeader=4 truncated to oho+4 must return None, not panic. | status=done (runtime fix in walkkit/src/probe/pe.rs:parse_pe; entry_point_rva read gated on size_optional_header >= 20. codewalk/src/probe/pe.rs fixed as dead-code mirror; optional_header_size_four_returns_none regression test in both crates.)
src/walker/mod.rs:244 | silent-truncation | medium | FileEntry::content() caps the total read at read_limit = self.size, the size CACHED during the directory walk. content_chunks() reads the live file, but the loop breaks once bytes.len() reaches the stale cached size (remaining==0, line 252). If a file GREW between walk and read (logs, active files), the appended bytes are silently dropped -> a scanner reading truncated content misses any pattern/secret in the grown region with NO signal (recall gap, Law-10-adjacent silent partial read). The comment at 238-239 claims it would "re-stat only if the cached size exceeds the limit" but no such re-stat exists. VERIFIED manually 2026-07-14. | Either read to real EOF (bounded only by MAX_CONTENT_AUTOLOAD, re-checked live) and drop the stale-size cap, or detect growth (chunk iterator still yielding after read_limit) and surface a truncation warning/flag on FileContent instead of silently stopping. Add a test: file grows after the FileEntry is built; content() must read the full current bytes or report truncation. | status=done (runtime fix in walkkit/src/codewalker/mod.rs:content; re-stats and reads to EOF bounded by MAX_CONTENT_AUTOLOAD. codewalk/src/walker/mod.rs fixed as dead-code mirror; file_content_reads_grown_file regression test in both crates, and codewalk/tests/test_depth_gap.rs now asserts full read.)

libs/runtime/codewalk/src/{detect,error,sandbox,probe,walker}.rs + Cargo.toml | DEDUPLICATION | high | codewalk's lib.rs is a pure re-export facade over walkkit, but src/ still carried the entire pre-walkkit walker impl (detect/error/sandbox/probe/walker + 3 test files) as dead uncompiled files, plus an unused hashkit dep | REMOVED dead source, dropped unused hashkit dep, kept walkkit facade, bumped 0.3.0->0.3.1 | status=done
src/codewalker/traverse.rs:135 | walkkit | silent-fallback | medium | REPOPULATE 2026-07-16: CodeWalker::count silently discards traversal errors via filter_map(Result::ok), returning an incomplete or incorrect file count without error notification | return Result<usize, CodewalkError> or surface errors loudly, with a proving test asserting that count returns an error or propagates a permission denied error rather than swallowing it | DUPLICATE (reconciled, dedup): this row's path `src/codewalker/traverse.rs:135` is a WALKKIT path, not a codewalk path — codewalk/src/lib.rs is a pure `pub use walkkit::{CodeWalker, FileEntry, ...}` facade (no walker code of its own). The fix landed in walkkit's own ledger (walkkit/BACKLOG.md `src/codewalker/traverse.rs:135`, status=done FIXED 2026-07-17: fallible `try_count()` propagates traversal errors, `count()` fails closed instead of `filter_map(Result::ok)`; proving tests + `cargo test -p walkkit --lib traverse` 9 passed). codewalk re-exports the fixed `CodeWalker`, so the acceptance criteria are met transitively with zero codewalk code change. | status=done
src/codewalker/mod.rs:248 | walkkit | performance | medium | REPOPULATE 2026-07-16: FileEntry::content reads file content in a loop calling content_chunks, which allocates a new 64KB vector on every single chunk read | read directly from the opened file into a single pre-allocated buffer or directly into the destination bytes vector, with a proving test asserting that content returns exact bytes and does not make per-chunk allocations | DUPLICATE (reconciled, dedup): `src/codewalker/mod.rs:248` is a WALKKIT path; codewalk is a pure re-export facade of walkkit. Fix landed in walkkit/BACKLOG.md `src/codewalker/mod.rs:248` (status=done FIXED 2026-07-17: `content()` reads straight into one pre-allocated buffer via `File::open(..).take(MAX+1).read_to_end(&mut bytes)`, no per-chunk 64 KiB Vec; proving test + `cargo test -p walkkit --lib content` 5 passed). codewalk re-exports the fixed `FileEntry`; criteria met transitively, no codewalk code change. | status=done